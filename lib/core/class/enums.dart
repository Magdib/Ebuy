enum StatusRequest {
  none,
  loading,
  offlinefailure,
  failure,
  serverfailure,
  success,
}

enum SearchState { searching, noRecent, recent, success, none }

enum PaymentState { noAddress, noMethod, none }
