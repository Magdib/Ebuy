import 'package:flutter/material.dart';

class PremierDelivery extends StatelessWidget {
  const PremierDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
